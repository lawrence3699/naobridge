<!-- Profile / Personal Center Page -->
<view class="profile-page">
  <!-- Registration flow (new user) -->
  <block wx:if="{{isNewUser}}">
    <view class="reg-title">完善个人资料</view>

    <view class="reg-card">
      <view class="form-group">
        <view class="form-label">昵称</view>
        <input
          class="form-input"
          placeholder="请输入昵称（最多20个字符）"
          value="{{nickName}}"
          bindinput="onNickNameInput"
          maxlength="20"
        />
      </view>

      <view class="form-group">
        <view class="form-label">身份标签</view>
        <view class="role-options">
          <view
            wx:for="{{roleOptions}}"
            wx:key="value"
            class="role-option {{selectedRole === item.value ? 'role-selected' : ''}}"
            bindtap="onSelectRole"
            data-role="{{item.value}}"
          >
            <text class="role-emoji">{{item.emoji}}</text>
            <text class="role-text">{{item.label}}</text>
          </view>
        </view>
      </view>

      <view class="form-group">
        <view class="rules-row" bindtap="onGoToRules">
          <view class="check-box {{agreedToRules ? 'checked' : ''}}">
            <text wx:if="{{agreedToRules}}">✓</text>
          </view>
          <text class="rules-link">我已阅读并同意《社区公约》</text>
        </view>
      </view>

      <button
        class="submit-btn"
        bindtap="onSubmitRegister"
        disabled="{{!canSubmit}}"
      >
        完成注册
      </button>
    </view>
  </block>

  <!-- Profile view (existing user) -->
  <block wx:else>
    <view class="profile-card">
      <image
        class="avatar"
        src="{{userInfo.avatarUrl || '/images/default-avatar.png'}}"
        mode="aspectFill"
      />
      <view class="user-info">
        <view class="user-name">{{userInfo.nickName}}</view>
        <view class="role-badge">{{roleLabel}}</view>
      </view>
    </view>

    <view class="menu-card">
      <view class="menu-item" bindtap="onEditProfile">
        <text class="menu-label">编辑资料</text>
        <text class="menu-arrow">›</text>
      </view>
      <view class="menu-item" bindtap="onGoToRulesView">
        <text class="menu-label">社区公约</text>
        <text class="menu-arrow">›</text>
      </view>
      <view class="menu-item menu-item-last" bindtap="onGoToAbout">
        <text class="menu-label">关于脑桥</text>
        <text class="menu-arrow">›</text>
      </view>
    </view>

    <view class="disclaimer-bar">
      本平台不提供医疗建议，内容仅供交流参考
    </view>
  </block>

  <!-- Loading state -->
  <view wx:if="{{loading}}" class="loading-mask">
    <text class="loading-label">加载中...</text>
  </view>
</view>
